drop dataverse ProductReviews if exists;
create dataverse ProductReviews;
use dataverse ProductReviews;

create type Review as {
    reviewerId: string?,
    asin: string,
    reviewerName: string?,
    helpful: [int],
    reviewText: string,
    overall: float,
    summary: string,
    reviewTime: string,
    unixReviewTime: bigint
}

create external dataset Reviews(Review)
using localfs(("path"="127.0.0.1://data/reviews_Books.json"),("format"="json"));

