drop dataverse TinySocial if exists;
create dataverse TinySocial;
use dataverse TinySocial;

create type reviewType as {
    reviewerId: string?,
    asin: string,
    reviewerName: string?,
    helpful: [int],
    reviewText: string,
    overall: float,
    summary: string,
    reviewTime: string,
    unixReviewTime: bigint
}

drop dataset reviews if exists;

create external dataset reviews(reviewType)
using localfs(
	("path"="127.0.0.1://data/reviews.json"),
	("format"="json"));

